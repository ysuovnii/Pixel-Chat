<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login — Pixel Chat</title>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body class="theme-anti-gravity page--auth">
    <!-- <%- include('layout', { user: typeof user !== 'undefined' ? user : null, activePage: 'home' }) %> -->

    <main class="page-shell">
        <section class="auth-scroll float-2">
            <header class="auth-scroll__header">
                <h1 class="auth-scroll__title">PIXEL CHAT — LOGIN</h1>
                <p class="auth-scroll__subtitle">
                    Insert access rune to descend into the star lobby.
                </p>
            </header>

            <% if (message) { %>
            <div class="error-box">
                <%= message %>
            </div>
            <% } %>

            <form action="/login" method="POST">
                <div class="auth-form__group">
                    <label class="auth-form__label">EMAIL</label>
                    <input class="input-field" type="email" name="email" autocomplete="email"
                        placeholder="hero@worlds.net" required />
                </div>

                <div class="auth-form__group">
                    <label class="auth-form__label">PASSWORD</label>
                    <input class="input-field" type="password" name="password" autocomplete="current-password"
                        placeholder="••••••" required />
                </div>

                <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 8px;">
                    <span class="pixel-icon">▶</span>
                    <span>JOIN SERVER</span>
                </button>
            </form>

            <div class="auth-scroll__footer">
                New traveller? <a href="/signup">Create an ID</a>
            </div>
        </section>
    </main>
</body>

</html>